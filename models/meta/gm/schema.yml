version: 2

models:
  # TODO: reimplement after refactoring model
  # - name: gm_by_contract_daily
  #   tests:
  #       - unique:
  #           column_name: "concat(date_day, contract_id)"

  - name: gm_weekly
    columns:
        - name: date_week
          tests:
              - not_null
              - unique

  - name: gm_by_contract_monthly
    tests:
        - unique:
            column_name: "concat(date_month, contract_id)"
